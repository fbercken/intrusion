apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "intrusion.fullname" . }}-injector-config
data:
  KAFKA_BOOTSTRAP_SERVERS: {{ .Values.innjector.kafka.bootstrapServers | quote }}
  KAFKA_TOPIC: {{ .Values.injector.kafka.topic | quote }}
      