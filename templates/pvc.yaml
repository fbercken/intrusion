apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "intrusion.fullname" . }}-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-csi 
  resources:
    requests:
      storage: 2Gi